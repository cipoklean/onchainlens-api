fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0
okxweb3-app-x402[evm,fastapi]==0.1.1
# cache-bust 2026-07-20: force fresh build so main.py x402 patches (get_supported + initialize + uppercase PAYMENT-REQUIRED header + direct base64 body) ship
